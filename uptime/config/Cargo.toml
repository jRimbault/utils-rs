[package]
name = "uptime_config"
version = "0.1.0"
edition = "2021"

[dependencies]
indexmap = { version = "2.14.0", features = ["serde"] }
serde = { version = "1.0.228", features = ["derive"] }
serde_with = "3.18.0"

[dev-dependencies]
toml = "1.1.2"
