Linux only TUI process-tree monitor for a single PID and its descendants.

```
prowl <pid> [--interval 1000] [--threads]
```

Keys: `q`/`Esc` quit, `↑↓` or `j`/`k` navigate, `Enter`/`Space` collapse subtree, `t` toggle threads.

